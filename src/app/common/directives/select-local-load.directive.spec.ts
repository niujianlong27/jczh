import { SelectLocalLoadDirective } from './select-local-load.directive';

describe('SelectLocalLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectLocalLoadDirective();
    expect(directive).toBeTruthy();
  });
});
